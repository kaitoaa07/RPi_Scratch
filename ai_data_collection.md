# AIデータ収集（Scratch拡張）マニュアル（ai_data_collection.md）

> **注意**: 本ドキュメントは日本語版Scratchを前提とします。  
> ただし本拡張には英語表記ブロック（`Start Pi Camera` など）が含まれます。

このドキュメントでは、Scratch（PCブラウザ）から Raspberry Pi 上のサーバーへ接続し、**Pi接続カメラ画像をラベル付きで保存**して、学習用データセット（`dataset.zip`）を作る手順を解説します。

---

## 1. 概要

- Scratch上で「現在のラベル（`forward / left / right`）」を選び、カメラ画像を保存します
- 保存数をラベル別に確認できます
- ラベル別ビューア（ブラウザ表示）で画像を確認できます
- `dataset.zip` として **全ラベル一括**でダウンロードできます

---

## 2. 前提条件

- Scratchは **PCのブラウザ**で開く
- Raspberry Pi 側サーバーは **起動済み**
- PiOLED に `Scratch:OK` が表示されている（起動・準備完了の目安）
- カメラは **Pi接続**
- ブラウザで `http://<PiのIP>:8601` を開ける（ネットワーク疎通確認）

---

## 3. 重要：データの保存場所（必ず読む）

この拡張で保存したデータセットは **PCブラウザのRAM上**に保持されます。

- ブラウザのタブを閉じる / リロードする / PCを再起動する  
  → **保存済みデータが消える可能性があります**
- 収集が終わったら、必ず **`dataset.zip をダウンロード`** してバックアップしてください

---

## 4. 用意しているブロック一覧（データ収集）

| ブロック | 機能説明 |
|---|---|
| `サーバーに接続する` | Pi側サーバーへ接続（最初に必ず実行） |
| `Start Pi Camera` | カメラ配信を開始 |
| `Stop Pi Camera` | カメラ配信を停止 |
| `現在のラベルを [LABEL] に設定` | 保存時に付与するラベルを設定（`forward/left/right`） |
| `画像保存` | 現在フレームを保存 |
| `保存数(ラベル別)` | ラベル別の保存数（JSON文字列）を返す |
| `データセット ([LABEL]) を開く` | 指定ラベルのデータ一覧（ブラウザ）を開く |
| `dataset.zip をダウンロード` | データセットを **全ラベル一括**でzipとしてダウンロード |
| `データセットをリセット` | 全データ削除（注意：元に戻せません） |

### オーバーレイ矢印（入力）
| ブロック | 機能説明 |
|---|---|
| `▲ボタンが押されたとき` | オーバーレイの上矢印が押された瞬間に発火 |
| `◀ボタンが押されたとき` | 左矢印が押された瞬間に発火 |
| `▶ボタンが押されたとき` | 右矢印が押された瞬間に発火 |

### モーター制御（任意：走らせながら収集する場合）
| ブロック | 機能説明 |
|---|---|
| `モーター [MOTOR] を速度 [SPEED] に設定` | -100〜100%で速度設定（モーター1〜4） |
| `モーター [MOTOR] を停止する` | 指定モーター停止 |

---

## 5. ラベル設計（固定）

本プロジェクトのラベルは3種類のみです。

- `forward`（直進）
- `left`（左）
- `right`（右）

---

## 6. データ収集の基本手順（最短）

1. Scratchで **拡張を追加**
2. `サーバーに接続する`
3. `Start Pi Camera`
4. `現在のラベルを [forward] に設定`
5. `画像保存` を繰り返して保存
6. `保存数(ラベル別)` で枚数確認
7. `データセット([LABEL])を開く` で画像を確認（ブレ・真っ暗・不要画像の混入チェック）
8. 各ラベル（forward / left / right）で 4〜7 を繰り返す
9. 最後に必ず `dataset.zip をダウンロード`

---

## 7. 収集のコツ（精度が上がる）

- **カメラ位置・角度を固定**（毎回ズレると別データ扱いになりやすい）
- **照明条件を変えすぎない**  
  - もし昼/夜が混ざるなら、その分だけ枚数を増やす
- **不要画像を減らす**  
  - ブレ、真っ暗、床以外が多い、手が映る、などは精度低下の原因になりやすい

---

## 8. サンプルスクリプト（最小）

### 8.1 1枚ずつ保存

```scratch
緑の旗がクリックされたとき
  サーバーに接続する
  Start Pi Camera
  現在のラベルを [forward] に設定

[スペース]キーが押されたとき
  画像保存
