# AI自動走行（Scratch拡張）マニュアル

> **注意**: 本ドキュメントは **日本語版Scratch** を前提としています。  
> Scratch画面左上の setting → language より [日本語] を選択できます。

このドキュメントでは、**Scratch × Raspberry Pi** 環境で、**Piに接続されたカメラ画像をAI（Pi側）で推論**し、推論結果（`forward / left / right`）に応じて **モーターを自動制御**するための拡張ブロックの使い方を解説します。

---

## 1. 概要

本拡張は、Scratch（PCブラウザ）からRaspberry Pi上のサーバーへ接続し、

- Pi接続カメラの配信（表示/非表示）
- AI推論（自動走行）の開始/停止
- 現在の予測ラベル取得（`forward / left / right`）
- モーター1〜4の速度制御（-100〜100%）
- モデル（`.tflite`）のアップロードと切り替え

をScratchブロックで扱えるようにしたものです。

---

## 2. 用意しているブロック一覧

| ブロック | 機能説明 |
|---|---|
| `サーバーに接続する` | Pi側サーバーへ接続（最初に必ず実行） |
| `カメラ配信を開始する` | Pi接続カメラの配信を開始 |
| `カメラ配信を停止する` | カメラ配信を停止 |
| `カメラ映像を表示/隠す` | Scratch画面上のカメラ映像を表示/非表示 |
| `自動走行(AI)を開始する` | Pi側でAI推論ループを開始 |
| `自動走行(AI)を停止する` | AI推論ループを停止 |
| `現在の予測ラベル` | 現在の予測ラベル（文字列）を返す |
| `forward` | ラベル定数（`"forward"`） |
| `left` | ラベル定数（`"left"`） |
| `right` | ラベル定数（`"right"`） |
| `モーター [MOTOR] の速度を [SPEED] にする` | 指定モーターの速度を設定（-100〜100%） |
| `モーター [MOTOR] を停止する` | 指定モーターを停止 |
| `すべてのモーターを停止する` | 全モーターを停止 |
| `モデルファイルをアップロードする` | `.tflite`モデルをPiへアップロード |
| `使用するモデルを [NAME] に設定` | 使用するモデル名を指定（例：`road_model.tflite`） |
| `現在のモデル名` | 現在選択されているモデル名を返す |

---

## 3. 前提条件

- Scratchは **PCのブラウザ** で開く
- 推論（AI）は **Raspberry Pi側** で実行する
- カメラは **Piに接続**している
- PiOLEDに `Scratch:OK` が表示されている（起動・準備完了の目安）
- サーバーは **起動済み前提**
- モーターは **1〜4** が接続済み（速度範囲：**-100〜100%**）

> 参考：4輪の場合の一般的な割り当て例  
> - **1=右前 / 2=左前 / 3=右後 / 4=左後**  
> ※あなたの機体で割り当てが違う場合は、サンプル中のモーター番号を置き換えてください。

---

## 4. 初期化手順（必須）

1. ブラウザで `http://<PiのIP>:8601` を開く（状態確認用）
2. Scratchエディタに本拡張を追加
3. **最初に必ず** `サーバーに接続する` を実行する
4. 必要なら `カメラ配信を開始する` → `カメラ映像を表示/隠す` で映像を確認
5. 必要ならモデルを準備（アップロード/選択）してから `自動走行(AI)を開始する`

---

## 5. 推論ラベル（重要）

`現在の予測ラベル` は、基本的に以下のいずれかを返します。

- `forward`
- `left`
- `right`

Scratchの比較では、文字列を直書きするより **ラベル定数ブロック**（`forward / left / right`）を使う方が安全です。

例：  
- `もし <現在の予測ラベル = (forward)> なら …`

---

## 6. サンプルスクリプト（基本：AI自動走行）

以下は、予測ラベルに応じて4つのモーター速度を変える最小例です。

> ポイント  
> - 判定ループに `待つ(0.05)`〜`待つ(0.1)` を入れると負荷が安定します  
> - `それ以外`（予測が空・不明など）は **停止** を推奨します（暴走防止）

```scratch
緑の旗がクリックされたとき
  サーバーに接続する
  カメラ配信を開始する
  カメラ映像を表示/隠す

  // （必要なら）モデル選択
  使用するモデルを [road_model.tflite] に設定

  自動走行(AI)を開始する

  ずっと
    もし <現在の予測ラベル = (forward)> なら
      モーター[1]の速度を(60)にする
      モーター[2]の速度を(60)にする
      モーター[3]の速度を(60)にする
      モーター[4]の速度を(60)にする

    でなければ もし <現在の予測ラベル = (left)> なら
      // 左旋回：左側を遅く、右側を速く（例）
      モーター[1]の速度を(70)にする
      モーター[3]の速度を(70)にする
      モーター[2]の速度を(30)にする
      モーター[4]の速度を(30)にする

    でなければ もし <現在の予測ラベル = (right)> なら
      // 右旋回：右側を遅く、左側を速く（例）
      モーター[2]の速度を(70)にする
      モーター[4]の速度を(70)にする
      モーター[1]の速度を(30)にする
      モーター[3]の速度を(30)にする

    でなければ
      // 予測が不明なときは停止（安全）
      すべてのモーターを停止する

    待つ(0.05)秒
